# 紧凑原始机甲首回合根

`CompactDiscardProjection` 现在分别审计战斗监听表与不可变牌组监听前缀。牌组钩子仅检查原版运行级派发能触及的方法，所有覆盖均须拒绝；不能把战斗实例的 Slither 抽牌处理错误套到牌组副本上。审计补齐伤害上限及牌堆变更、受伤、死亡的晚阶段钩子。共享缓存只保留类型／方法信息。

蛇之戒只修改首回合起手抽牌，当前程序从玩家行动边界开始，因此允许将其作为不变根模型保留。这不表示紧凑起手或回合推进已实现。其他遗物和未知钩子仍受整根准入约束。

## 验证

`COMPACT-FULL-ROOT-NATIVE` 使用前轮机甲完整样例的原始 30 张牌、升级与 Slither 附魔、蛇之戒、种子 `VH_PERF_MECHA`、敌人 300 HP、玩家 65/65 HP。没有清除遗物或裁剪牌组；进入夹具时原生已完成起手抽牌，保留 31 个牌组监听器、7 张手牌和 3 能量。完整请求与逐牌证据见[JSON](simulation-full-root-20260911.json)。

- `7ef49b1ef6de4b56a82c6e6c180754b3` Passed，24.32 秒。七张起手分别出牌、两条正反序续接，共 9 分支；完整 Snapshot／原状态键／估值／Power 顺序与字段／来源历史／九 RNG、逆序恢复、缓存开关及八工作区一致。反序分支包含生成并按倒序打出的两张附魔小刀。
- 原生依次打出 SUPPRESS、BACKFLIP、STRIKE_SILENT、MIRAGE、ESCAPE_PLAN、TOOLS_OF_THE_TRADE，共六步逐字段严格对照；原生推进后重新读取冻结根仍等于初始完整状态。
- 首次请求 `ec00db2a6098433592d0b075e93c9303` 因启动脚本默认额外注入一张牌，在牌数断言处失败；显式 `--cards-json '[]'` 后使用同一构建通过。没有把该准备失败归为语义偏差。
- Release 10.15 秒、0 警告／错误。测试实例 956969 已停止；Linux 结构门禁 `search_files=88`、293 个本地链接与两个 JSON 记录通过；PowerShell 同步但未运行。

本项只证明完整首回合根和上述行动分支，不是正常搜索测量或整场部署。带弃牌选择的完整枚举、灼伤、敌方行动与回合推进、第二份原始样例和生产 A/B 仍待完成。
