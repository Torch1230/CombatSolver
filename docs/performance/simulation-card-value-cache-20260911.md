# 卡牌基础估值缓存：完整输入复评（2026-09-11）

[性能目录](README.md) · [全部样本与路线对照](simulation-card-value-cache-20260911.json)

**撤回，不加入生产。** 这是[早期短搜缓存实验](veryhigh-state-experiments-20260908.md)的完整输入复评，不是新的缓存机制。复用预览 COW 的候选保持了路线，但收益仍约 2%，分配略增，GC 没有稳定改善。后续投入继续面向执行和状态表示。

## 改动与正确性

基线为 `3ce18ed`。候选把原 `CardValue` 公式的结果存入 `PredictedCard.PreviewStorage`；Fork 共享不变结果，取得可写预览时失效。外部卡牌类型与不透明附加模型旁路，实际费用和全局关键词继续实时计算，未改变评分公式、状态键或预算。

Release 构建 0 警告／错误。最小合同 `562da65df4654273bd870a0d5de7a622` Passed，23.30 秒，覆盖原公式、零值、升级、动态变量、父子／兄弟隔离、八路共享读取、不透明模型旁路和 live 不变。之后草拟的自定义 Type 测试未构建，不计为通过；全部实验源码已撤回。

## 完整对照

预先固定 ABBA，每个独立 headless 进程先预热完整机甲和亡灵输入，再依次测量两场。保持原完整 30／38 牌、附魔、遗物与药水配置，VeryHigh／DOP8／NoGC16GB；无节点、时间、Beam 或选择预算覆盖，无增量验证，每请求上限 120 秒。

| 完整请求 | A 耗时均值 | B 耗时均值 | 两组配对变化 | worker 分配变化 |
| --- | ---: | ---: | --- | ---: |
| 机甲 | 2.970 秒 | 2.910 秒 | −1.78%、−2.31% | +0.018% |
| 亡灵 | 75.320 秒 | 73.881 秒 | −1.58%、−2.24% | +0.021% |

八次测量均 Passed。每场全部 88 项逻辑 RESULT 字段及完整动作、回合结果和预测相同；机甲 119,407 次转移，亡灵 1,693,024 次。没有减少搜索工作来取得时间差异。

亡灵的 GC 总暂停均值由 2.323 秒变为 3.157 秒，最坏单次暂停 A 为 2.079 秒、B 为 2.080 秒。该小样本没有证明暂停改善，也不能把较低的采样 RSS 解释为缓存节省了堆空间：峰值包含同一进程的两场预热和之前请求残留。只有两组配对，基线自身也有约 2% 漂移；不据此承诺稳定的大幅收益。

## 收口与复跑范围

实验产物、生产补丁、命令和原始日志保存在本地 `.local/production-evaluation-20260911/`；提交的 JSON 保留全部测量、逻辑字段、路线与具体命令。分析脚本首次缺少 `json/re` 绑定，修正后读取已有日志，未重跑游戏请求。全部测试实例已停止。

当前源码不含该缓存或专用测试入口，不能用 HEAD 重跑并冒充候选 B。没有运行新的完整原生部署、可见 Steam、发布或推送。本轮记录只支持撤回决定；不增加生产加速声明。
