# 极高8/16路的有界决策筛查（2026-09-22）

[性能目录](README.md) · [逐次证据与撤回补丁](bounded-quality-screen-20260922.json)

**本批三个候选均没有改善最终决策，全部撤回或保持关闭。相对 `8b3a7983` 没有新增生产行为，也没有达到额外20%速度或显著质量提升目标。** 既有内存收益不重复计入。

## 范围与结果

Linux、.NET SDK9.0.120、7840H（8物理/16逻辑核），VeryHigh Beam135、完整Coordinator、宽度组合、Smart药水、16 GiB NoGC、DOP8/16。请求时间覆盖60秒，进程上限120秒；节点配置为10,000或50,000，**不是默认500,000节点**。节点配置也不是组合与审计的请求总展开上限。

共12个候选对照，覆盖6个不同根、五角色和药水哨兵；新执行15个进程，复用此前6份基线结果及本批基线。重复使用同一根不算新的泛化样本。逐对完整路线、质量及开局根均相等，未观察到宿主时间边界或Coordinator deadline日志。

| 候选 | 对照 | 结果与处置 |
| --- | --- | --- |
| 回收能力成员未用节点额度 | Regent首领、Silent精英、Ironclad留出；50,000节点，DOP16/16/8 | 3对展开和转移均相同，战损40/41/21均不变。后续成员不需要更多额度，撤回。 |
| 现成的连续威胁开关 | 六根；10,000节点，DOP8/16各3根 | 6对完整质量相同，总转移逐根增加；仍关闭。该开关还会旁路默认组合再分配，不能把变化全归因于评分公式。 |
| 资源动作后强制能力前缀 | Silent精英、Ironclad留出使用50,000；Defect精英使用10,000 | 前两根分别新增4/3条实际求解路线，但没有胜出；第三根没有新增能力路线。额外成本没有换来质量收益，撤回。 |

资源前缀原型复用实际模拟和已有动作枚举，没有卡牌名称或场景特判。保留原能力成员及其预算、顺序和评分；追加成员只用普通宽度，领取旧能力阶段剩余节点，无旧前缀时共享一份profile额度。该额度限制没有保证实际计算成本不增加。资源枚举还发生在追加阶段计时前，生产化需要补齐计时与取消检查；本批没有把该原型作为可发布实现。

| 资源前缀样本 | 原版→原型秒 | 耗时变化 | RSS峰值变化 | 战损 |
| --- | ---: | ---: | ---: | ---: |
| Silent精英，DOP16 | 30.279→33.912 | +12.00% | +11.65% | 41→41 |
| Ironclad留出，DOP8 | 53.465→58.739 | +9.87% | +9.10% | 21→21 |
| Defect精英，DOP16 | 7.628→7.749 | +1.58% | +0.72% | 13→13 |

这些数字只用于否决候选，不是隔离ABBA的稳定性能结论。预算回收的Silent候选测量期间误并行运行了一次约0.594秒的Python额度算术检查，该臂耗时/内存不用于结论；原始值保留在JSON。其余筛查没有与构建、测试或采样器并行。

## 并行调查的边界

现有lane已经覆盖动作、药水、回合尾部、首层选牌回放、StandPat和部分保路计算。当前Tail在动作派发完毕后即可派发，不需要等动作全部完成；旧文档中的串行依赖不能直接当作当前优化空间。同父Fork会写入COW标记和历史，仍需保护。评分预计算是可并行的局部点，但没有证据显示其成本足够支撑20%完整请求收益。

还检查了按能力前缀终态去重：仅StateKey不足以涵盖累计质量和动作历史，不能直接合并。原版单人能力估值登记覆盖104张，没有发现可用“删除登记过滤”修复的现成遗漏。这两项只读调查没有产生生产修改。

## 恢复与复现

两份源码原型补丁、每次命令、指标、质量、差异及成员日志保存在JSON；原始文件位于`.local/power-budget/`、`.local/vh-threat/`、`.local/resource-power/`。主要基线为`8b3a7983`；复用的`opening-prefix/corpus`候选与它行为相同，仅测试helper的文件归属尚未调整，不用于严密二进制计时结论。

生产源码已恢复；Release构建零警告/错误，`CopyModOnBuild=false`。没有对被否决原型追加原生验收，也不重复运行已通过且输入/行为未变的合同。本批没有Windows、可见Steam/FPS或整场原生执行证据，没有部署或发布。
