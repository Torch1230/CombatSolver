# 洁净与来生的值执行

精确 `Cleanse`（洁净）与 `Afterlife`（来生）现进入已有宠物根的紧凑执行域。洁净先召唤，再从抽牌堆选择消耗；来生完成召唤后消耗自身。升级量从根变量捕获，复用既有宠物值命令、牌堆移动和消耗历史，没有新增旧引擎补偿。

`ExhaustFromDraw` 拥有独立挂起位置，选择恢复推进下一条指令，不会重复召唤。计划桥接根据效果区分卡牌消耗和 Stratagem 洗牌检索，保留来源、顺序、实例 token 与正式游标校验。空抽牌堆不请求选择；单张仍对账旧计划中的隐式选择。可达消耗通过 `ExhaustsCards` 关闭存活牌集合不变缓存，避免使用消耗前的牌库估值。

| 本轮场景 | 结果 | 验证范围 |
|---|---|---|
| `COMPACT-DRAW-EXHAUST-NATIVE` | Passed，28.50 秒 | 三个根（四张／单张／空抽牌堆，存活／死亡宠物）；每根两张洁净、两张来生及两个完整回合，共 18 原生动作、39 分支、15 个省略选择边界；两次原生选择前观察证明召唤已完成 |
| `COMPACT-DRAW-EXHAUST-SEARCH` | Passed，5.88 秒 | 固定 250 节点，旧／新 DOP1、紧凑 DOP2 完整搜索结果一致，实际并发 2；非零挂起、取消／异常排空、失败后根复用、未知药水拒绝及实机不变 |

逐步验证完整快照、能力元数据、九 RNG、历史来源、全部估值字段及原键／续用，覆盖缓存开关、逆序恢复、撤销、八工作区和实机推进后冻结根不变。原生牌堆比较使用根身份与生成历史身份，包含第二回合怪物生成的四张灼伤，并核对所有生成卡元数据。单张原生选择直接执行，未触发观察回调；其计划消费和结果身份通过，不能计作原生选择页面观察。

v1 在模式 0 第二回合的牌序核对失败：夹具把四张新生成灼伤都记为 -1。此前卡牌／回合完整快照和宠物标量已一致。v2 补齐夹具生成身份及元数据检查；随后审查补上消耗缓存失效，形成最终 v3。全部失败、三个成功构建和最终两个 runId 保存在[结构化记录](simulation-draw-exhaust-20260911.json)。最终构建 11.31 秒，零警告／错误；本轮没有修改旧模拟器语义或镜像登记。

重跑沿用[宠物值执行命令](simulation-osty-values-20260911.md)，替换产物目录及上述 ScenarioId，保持 SILENT／MECHA_KNIGHT_ELITE、Cards=[]、敌人 300 HP、Instant、120 秒上限。Windows 使用对应 PascalCase 参数，无脚本协议变化。

首次宠物创建、随机插入抽牌堆与更多观察者仍未准入；原始亡灵完整输入及动态闭包、最终预热交错性能验收继续进行。请求总时长包含建局，不是加速数据。本轮没有启动 Steam、安装、打包、发布或推送。

Linux 结构门禁通过（93 个 Search 文件）；PowerShell 等价维护但未执行。317 个本地文档链接与变更 JSON 检查通过。
