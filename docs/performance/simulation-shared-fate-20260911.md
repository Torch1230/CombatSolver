# 命运同担与属性能力类型（2026-09-11）

命运同担进入紧凑闭包，现准入 52 种精确卡牌。编译器按原生顺序生成两条既有基础能力指令：先对玩家施加负力量，再对指定敌人施加负力量；普通／升级分别削减敌人 2／3 点，自身均为 2 点。消耗、人工制品、能力归零与重获、施加者及迅速附魔都复用原有分支执行，不增加卡牌专属值槽。

首次完整能力差分发现，基础布局在玩家重新获得负力量时错误写入首次持续计数跳过标记。原生 `PowerCmd.Apply` 依据能力的 `Type` 创建该标记，而人工制品依据 `GetTypeForAmount` 判断请求量：力量／敏捷类型仍是增益，负请求却会被人工制品视为减益。现分开两个判断。旧引擎此处原本正确；失败基线 `9dabae2967604e81ac8bafe0ed74166c` 在完整生命周期对照中检出，未放宽快照、状态键或比较器。诊断异常现在携带阶段和两侧能力字段。

三种根各自执行普通命运同担、带迅速的升级牌、玩家打击、奥斯蒂出击、生存者弃预备自动出牌，再推进两个完整回合。共 21 次原生动作、42 个候选分支、18 个省略选择边界、六个完整回合；三个原生迅速选择现场都已完成双方力量削减。输入分别验证正力量归零后重获负值、双方各一层人工制品先拦截、已有负力量继续降低；主人与目标分别保留施加者，奥斯蒂力量不被错误修改。

每个候选比较完整状态、逐实例能力生命周期、有序牌堆、九 RNG、来源历史、全部估值、原键及续用；另核对八工作区、逆序恢复、撤销及实机推进后重算冻结根。固定 250 节点搜索比较旧／新 DOP1、新 DOP2，验证并发、选择挂起、取消／错误排空、根复用、未知药水拒绝和实机不变。

| 场景 | 结果 | 请求耗时 |
| --- | --- | --- |
| `COMPACT-SHARED-FATE-NATIVE` | Passed | 30.10 秒，含进程启动 |
| `COMPACT-SHARED-FATE-SEARCH` | Passed | 5.00 秒 |

纯值全套合同通过，补充玩家负力量、负敏捷及虚弱的移除／重获向量：三者的请求均为减益，仅虚弱创建持续跳过标记。最终 Release 构建 v3 为 11.22 秒，零警告／错误；Linux 结构门禁通过（93 个 Search 文件），PowerShell 等价规则已维护、未执行。旧镜像登记与 CoverageCatalog 分类未变，未运行目录验证。[原始结果、原生样本及构建证据](simulation-shared-fate-20260911.json)。

复现沿用[神气制胜阶段命令](simulation-panache-20260911.md)，替换本批产物及表中 ScenarioId；SILENT／MECHA_KNIGHT_ELITE、HP300、Instant、120 秒，同一 headless 实例复用。各夹具自行注入所需状态。

原始亡灵完整输入尚有两种卡牌、动态生成及遗物／药水／敌人闭包，最终预热交错性能验收也未完成。本批请求耗时不作为加速结论；未启动 Steam、安装、打包、发布或推送。
