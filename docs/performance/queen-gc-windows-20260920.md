# 女王 NoGC 有效窗口的 Windows 验证（2026-09-20）

用户指定 Windows 仅作测试端，产物带回 Linux 工作区，交付前清理远端测试文件、实例、进程和本次崩溃转储。生产源码不变；基线 `967c7c39`，候选 `5f115aba`。

## 范围与环境

使用同一女王问题包 `46b87aaad3b14174a90ab975b0ae637d` 的 `start` 快照，游戏 v0.111.0、RitsuLib 0.6.2、.NET 9.0.19。Windows 11专业版、i7-14700KF；测试机内存34,185,609,216字节（约32GiB）、28逻辑处理器。通过SSH运行仓库PowerShell无头入口，未启动可见Steam会话；结果不能作为可见帧时间或FPS证据。

两版Mod和CLR检查程序集在Linux构建后复制到专用测试目录；Windows未作为开发或交付目录。每请求独立游戏进程、独立冻结实例，`CleanupInstanceOnExit`负责清理。仅测试端启动器追加退出前保存日志的动作，不修改游戏或Mod测试路径。

## 真实CLR回归

旧版`commit-window`在“完成回收后不得重新建立仅约55MB有效窗口或保留限额”断言失败。候选同条件通过，日志确认853,962,871字节区域、513,894,480字节预留被拒绝，并在较小预留下恢复。恢复10项、恢复生命周期2项、检查点1项、准入6项也全部通过，合计20项。

旧版预期断言失败由Windows生成`dotnet.exe.106272.dmp`。已在本地读取转储中的命令行，确认它属于本次`checks-baseline`进程；远端清理范围包含该文件。

## 原包恢复与12GB试验

候选`RestoreOnly/start`通过，`continuationVerified=true`。旧包缺少原生编号映射，`nativeStateVerified=false`，保留`legacy_model_id_mapping_not_recorded`限制。

首次命令在启动前被PowerShell参数的DOP1–8白名单拒绝。随后通过现有`ReplayPolicyOverridePath`指定DOP16，日志及短搜指标证实实际并行度为16；未绕过生产验证或修改生产代码。

两版均以Beam512、100,000节点上限、60秒搜索预算、DOP16、NoGC12,000,000,000字节尝试请求，在120秒总超时处由启动器终止。没有完整result，因此不比较搜索质量或计算提速百分比，也未扩大同一fixture的总超时。

保留下来的日志中，两版都有大区域重建和秒级GC暂停；旧版观察到单次最大暂停8,331.8ms，新版7,181.5ms。这只是两个不完整请求中的观测值，既不能解释为可见帧收益，也不能证明稳定改善。旧版日志为运行中拷贝的前缀，新版为强制结束后的磁盘日志；缓冲尚未落盘的事件可能缺失，不能将日志条数当作请求总次数。两份日志已完成的各5条回收记录均未显示`commit_window_insufficient`。

## 有界短搜对照

改用每主成员1,000节点、请求30秒预算、DOP16、NoGC1,000,000,000字节，仍为同一原包、Beam512、原Smart药水政策。按A-B-B-A独立进程顺序完成4份请求。

| 顺序 | 搜索累计耗时 ms | worker累计分配 B | GC累计暂停 ms | 主动回收 |
| --- | ---: | ---: | ---: | ---: |
| short-a1 | 14232.5 | 4860081032 | 734.4 | 8 |
| short-b1 | 13475.8 | 4858229152 | 38.8 | 8 |
| short-b2 | 13420.1 | 4853710584 | 35.6 | 8 |
| short-a2 | 13164.2 | 4855828752 | 35.5 | 8 |

四次均Passed/NodeLimit，主成员1,000展开、14,222转移；整个请求均4,200展开、123,022转移、78,282选牌分支，实际最大并行度16。均未获胜、预计战损15、最终HP45、敌方剩余HP614、不用药。

两对全部非时序指标（含完整portfolio成员状态）和执行政策一致；逐项比较每份29条`ROUTE_REPLAY/ROUTE_ACTION`记录，去除随机traceId后动作、选牌与逐动作HP/格挡/能量/星能/手牌数对账完全一致。该回放只核对指定标量，不替代完整原生状态差分、完整女王路线或整场战损验收。四份日志均完整解析并记录到`host_exit`。

第一对候选耗时−5.3%，反向一对+1.9%；均值13.698→13.448秒（−1.83%），方向不稳定。首个基线GC暂停明显较高，另外三次接近；所有请求均8次主动回收、8次重建，日志均未出现小窗口拒绝。**不声称已证明稳定提速，也不将这组波动归因于新准入规则。**

## 清理与证据

原始日志、请求、政策、结果、测试脚本和环境记录已带回本地`.local/windows-queen-20260920/evidence.zip`，分析脚本为同目录`analyze.py`；精简数据见[结构化证据](queen-gc-windows-20260920.json)。7个已启动游戏请求（恢复1、超时2、短搜4）均由启动器删除实例。

01:15（Asia/Shanghai）清理操作确认：远端专用目录`C:\temp\codex-queen-20260920-5f115aba`已删除；清理前本任务进程0、残留实例目录0；本次`dotnet.exe.106272.dmp`也已删除。专用目录内的上传包、冻结游戏副本、DLL、脚本、临时文件、日志、证据ZIP和host租约全部随目录删除。测试使用源游戏的只读快照，未替换用户正式Mod。

未验证完整300秒原机长搜、女王整场自动部署或可见帧时间。精确小窗口修复已在Windows真实CLR上得到直接证据；游戏短搜并未触发该拒绝路径，不能将其计时差异归因于修复。
