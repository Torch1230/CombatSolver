# 性能优化正式 PR：当前上游验收（2026-09-15）

本批完成通用分配优化、生成池与抽牌前缀复用，以及41张原版单人卡、9种手动选牌药水、回合来源和嵌套执行的三阶段续接。当前整合基线为上游 `cd66b1d`（0.38.6）；这份报告单独记录合并后的验证与整批对照，历史分阶段收益不相加。

## 最终实现与评审入口

- RNG 未访问流共享不可变状态，已访问流在Fork时捕获；卡牌首次入场检查按wrapper保存，冻结跑局监听前段精确共享，均匀资源池跳过无消费者的排名暂存。
- 原生角色生成池按已核对的过滤规则冻结有序候选；回合抽牌前后稳定前缀复用保留原RNG、来源顺序和预算。
- 41张卡牌与9种药水的自身选择共用唯一结算入口；已适配的回合、抽牌、自动及重复子出牌保存纯数据帧，可以继续遇到选择并再次挂起。
- 同一Fork context重映射卡牌、CardPlay、历史、trace、Power来源和死亡集合；选择层/frontier持有检查点并在工作排空后释放。未知派发、历史或不透明事务继续原完整回放。没有保存CLR栈、Task或闭包。

原生逐字段、父子与兄弟隔离、DOP、取消/异常和全部来源合同见[三阶段实施记录](choice-continuation-expansion-implementation-20260914.md)。早期通用优化见[首批报告](general-allocation-20260914.md)，生成池与抽牌前缀见[后续报告](crab-latency-20260914.md)，三牌独立原型见[原型报告](choice-continuation-prototype-20260914.md)。这些是各自源码与输入的历史证据，不能当作当前上游的重复验证或累计百分比。

## 合并处理与新验证

上游0.38.3–0.38.6的成长早停、复活成本、格挡药直插及其他功能保留。三处滚动文档冲突保留双方记录，生产源码自动合并后通过正常Release构建，14.56秒、0警告/错误。

| 合并后合同 | runId | 结果 |
| --- | --- | --- |
| 卡牌扩展严格增量 | `ae00c0b92e1b485b83c04c65328d5441` | Passed，25.959秒 |
| 药水严格增量 | `a8a8a40b79b548659aab223a2220caf4` | Passed，5.228秒 |
| 动作/EndTurn嵌套严格增量 | `5feed9b8a4ad481096bd03980af551d0` | Passed，4.785秒 |
| 首回合准备严格增量 | `358e83a8965841db9f40e7c5d2562ef8` | Passed，4.103秒 |
| 原生跨回合连续选择 | `98135e17d0c84660b9bc5958547180af` | Passed，5.872秒，35分支与完整状态对账 |
| 上游成长目标与药水早停 | `3a599fbe370848258b538fa12fde2e7a` | Passed，4.915秒 |
| 格挡药直插原生整场部署 | `36e20d691de1424e9b5e7e14196623ce` | Passed，25.337秒，省9血、T2无伤获胜、计划外重算0 |

上述请求均为Linux无头，120秒上限；实际部署使用Instant/0秒。整合后的Bash与PowerShell结构门禁均通过，`search_files=102`。比较器源码未变化，复用此前16项通过证据。

上游格挡药夹具首次运行 `7f79df25a74f486aa7388e33a20fcc28` Failed：无药路线只掉4血，不满足夹具自己断言的9血插入门槛。给敌方明确注入5层力量后覆盖恰好9血门槛，保留全部原胜利、省血及用药断言，并将请求上限改为120秒。没有修改生产门槛。Linux入口同步新增上游已有的三态插入断言；Bash门禁补入该partial文件与PowerShell已有的直插规则。原Bash门禁的缺失清单失败保留，不记为通过。

## 当前上游完整性能对照

基线只向上游 `cd66b1d` 的独立构建补入相同Testing能力：显式 `fixedSearchBudget:false` 和NoGC正常回退断言。未加入任何生产优化。双方只在计时结束、指标冻结后导出完整结果；正常构建用于上述语义与部署合同。首个未补齐测试能力的A1/crab在建局前因未知fixedSearchBudget字段被拒绝，未进入搜索，不计入性能样本。

沿用原蟹战、扩展弃牌与携药轻场景输入，每场R1/C1/C2/R2交错；每版本两次，每次独立进程。保持原VeryHigh、DOP16、16GB NoGC及全部搜索预算，完整请求上限420秒。测量期间不运行构建、其他测试或采样器。峰值RSS来自同一结果PID；比较完整输入、政策、全部动作/路线与默认严格的结果字段。

12份有效请求全部通过；每场完整输入、政策、动作、路线和归一化工作量严格相同，没有新增比较器排除项或缩小搜索预算。以下为每版本两次的均值，比较整个PR与当前上游。

| 场景 | 平均耗时：上游→候选 | 累计分配变化 | 峰值RSS变化 | 完整动作/工作量 |
| --- | --- | --- | --- | --- |
| 原蟹战 | 36.347→30.550秒（-15.95%） | -15.51% | -1.64% | 63动作、T16、战损49，严格相同 |
| 扩展弃牌 | 4.637→4.386秒（-5.42%） | -9.61% | -6.35% | 41动作、T1、战损0，严格相同 |
| 携药轻场景 | 0.616→0.602秒（-2.25%） | -11.55% | -1.50% | 5动作、T1、战损0，严格相同 |

| 场景 | R1上游：秒/峰值GB | C1候选：秒/峰值GB | C2候选：秒/峰值GB | R2上游：秒/峰值GB |
| --- | --- | --- | --- | --- |
| 原蟹战 | 35.473 / 16.733 | 30.817 / 16.148 | 30.283 / 16.519 | 37.221 / 16.478 |
| 扩展弃牌 | 4.708 / 4.553 | 4.380 / 4.261 | 4.392 / 4.278 | 4.566 / 4.565 |
| 携药轻场景 | 0.620 / 1.763 | 0.601 / 1.719 | 0.603 / 1.807 | 0.612 / 1.815 |

GB按十进制。蟹战GC暂停上游两次为2.262/4.022秒，候选为1.743/0.028秒；波动明显，不据此承诺稳定GC暂停或可见卡顿改善。两个轻场景均没有GC暂停。全部样本、质量字段和比较结果见[结构化证据](performance-pr-20260915.json)。

当前上游蟹战两侧均为49战损、T16，与旧0.38.2报告的40战损、T15属于不同源码基线，不能混合比较。当前这组三场属于严格同工作量对照；旧三阶段蟹战的工作量差异仍保留在历史报告中。

三场选中结果载体的药水前缀复用计数均为0；这些数据证明整批组合的请求表现，未建立药水检查点的独立整搜收益。九种药水的真实搜索命中与原生功能由专用合同证明。


## 交付边界

本批沿用合入上游的0.38.6元数据，没有单独提升版本或创建发布包。可见Steam、Windows帧时间与卡顿没有测试；当前无头数字不能外推这些结论。原有覆盖合同不等于穷举所有战斗或第三方状态。
