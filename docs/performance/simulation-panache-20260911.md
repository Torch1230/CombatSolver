# 神气制胜独立实例与终局分派（2026-09-11）

神气制胜进入紧凑执行闭包，现准入 51 种精确卡牌。每个能力实例分别保存数量、施加者、获得顺序、回合起始数量、剩余出牌数、首次应用标记及持续标记；实例数与字段属于同一可增长撤销缓冲区。普通能力与独立实例共用获得顺序。完成读取器只在超过 lane 历史最大实例数时扩容，恢复较少实例时停用多余模型；导入私有动态变量和 StateStore，不重放施加或 Hook，也不把模型带入候选。

旧引擎把第二张神气制胜叠到第一个实例：普通 10 与升级 14 被合成 24。八张原生牌的失败基线 `73d3e6f7ebee4925b7b7cf52187a7b8b` 在第二次施加发现该差异。修复使用明确的独立施加入口，共用原有命令门禁、施加修饰和数量回调；旧其他能力入口未改。首次应用标记影响下一张牌是否减计数，现进入状态键及完整续用，不能把该差异合并为同一搜索状态。

每张牌完成历史先于出牌后伤害；投影保留父牌和能力方法来源。AfterCardPlayed 是原生明确绕过结束门禁的完成回调。因此卡牌自己击杀最后敌人后，神气制胜仍计数；第一个神气制胜实例击杀时，后续实例也仍重置计数，但无目标可伤害。回合末计数重置保留首次应用标记，主人死亡清除根与后来新增的实例。

失败终局还暴露了两个回合边界偏差。紧凑程序原先在玩家回合末第二阶段后立刻提交失败，早于原版的敌方开始安全点；修复后仍切换阵营、捕获起始能力数量并清除敌方格挡。旧引擎此前却在已等待失败时继续补偿新一轮中毒，预测敌人 HP9997，原版 HP10000。现在按原生 Hook 分派入口检查 IsOverOrEnding，在整个已开始的分派内不逐监听器重查。中毒与格挡后回调不启动，保留原有能力数量；死亡清理仍完成。胜利和失败测试分别在 EndCombatInternal 与 ProcessPendingLoss 入口观察原生清理前状态。

| 场景 | 结果 | 请求耗时 |
| --- | --- | --- |
| `PANACHE-INSTANCES` | Passed | 24.32 秒，含进程启动 |
| `COMPACT-PANACHE-NATIVE` | Passed | 31.51 秒，含进程启动 |
| `COMPACT-PANACHE-TERMINAL` | Passed | 4.08 秒 |
| `COMPACT-PANACHE-SEARCH` | Passed | 4.92 秒 |
| `COMPACT-PANACHE-PLAYER-DEATH` | Passed | 24.64 秒，含进程启动 |
| `COMPACT-PANACHE-CARD-TERMINAL` | Passed | 24.55 秒，含进程启动 |

三种普通路线根分别注入零、一、两个初始实例，并各自再施加普通／升级两张牌。总计 24 次原生动作、45 个候选分支、18 个省略选择边界、六次完整回合推进。迅速抽牌的三个原生选择现场均观察到新实例已经创建，但尚未完成自身首次出牌；生存者弃掉预备后自动出牌，验证子牌与父牌完成顺序。比较完整快照、逐实例能力生命周期、有序牌堆、九 RNG、来源历史、全部估值、原状态键和续用，另测逆序恢复、撤销、八工作区和实机推进后重算冻结根。单标记见证只改变 AlreadyApplied，原键不同且未来伤害不同。

固定 250 节点比较旧／新 DOP1、新 DOP2 的完整结果与续用，实际并发 2，包含非零挂起、取消／异常排空、原根复用、未知药水拒绝和实机不变。纯值合同包含多个目标、嵌套子牌、独立计数、300 个新增实例、撤销和八工作区；毁灭合同补充待失败时切换敌方、清格挡及跳过新回调。敌方下回合格挡只在纯值内核合同验证，整根准入仍拒绝该能力主人组合，不能据此扩大原生覆盖。

验证过程保留了失败证据：原生独立施加差异；初版投影使用类型名作为能力伤害来源而非原模型 ID；过早提交失败；旧中毒补偿；测试尝试注入未准入的敌方下回合格挡而被拒绝；以及对出牌完成回调过度套用结束门禁而被旧引擎对照检出的中间版本。最终实现保留原生明确列出的完成回调例外。修复没有放宽比较器、绕过未知语义或扩大搜索预算。最终源码相应测试通过后复用已有证据，没有重跑输入与行为均未受影响的场景。

最终 Release 构建 v9 为 11.76 秒，零警告／错误。Linux 结构门禁通过（93 个 Search 文件）；PowerShell 等价规则已同步，未执行。镜像登记及 CoverageCatalog 分类未变，未运行目录验证。[原始结果、失败基线、原生样本与构建日志](simulation-panache-20260911.json)。

复现使用本批 Release DLL 与 manifest 独立目录，SILENT／MECHA_KNIGHT_ELITE、HP300、Instant、120 秒：

```bash
./tools/run-unattended-test.sh --scenario-id COMPACT-PANACHE-NATIVE \
  --character-id SILENT --encounter-id MECHA_KNIGHT_ELITE --enemy-current-hp 300 \
  --timeout-seconds 120 --headless-instance compact-panache \
  --combat-solver-build-dir <artifact> --evidence-directory <evidence>
```

替换表中 ScenarioId 可运行各场景；Windows 使用对应 PascalCase 参数的原生 PowerShell 入口。各夹具自行创建手牌、能力及其他所需状态。

原始亡灵完整输入还需其余三种卡牌、动态生成、遗物、药水和敌人闭包，以及最终预热交错性能验收。请求耗时不作为加速结论。本批仅用 headless，未启动 Steam、安装、打包、发布或推送。
