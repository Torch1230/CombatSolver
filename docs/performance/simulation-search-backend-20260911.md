# 紧凑状态接入搜索候选：2026-09-11

本阶段把已通过原始 30 牌完整原生路线对照的值状态接入现有搜索。`SearchPolicySnapshot.CompactRoot` 默认空，仅 `COMPACT-SEARCH-BACKEND` fixture 在主线程捕获并注入；Runtime 尚未选择它。原始亡灵契约师输入及完整支持闭包仍未完成。

## 所有权与执行

- `Prediction/Compact/CompactCombatRoot` 捕获模型闭包、初始冻结值和共享根；旧根 Fork 的 COW 发布用窄锁保护，不锁事件解码、完整回放或搜索。
- 每个固定搜索 lane 独占值工作区、计划解析与完整评分读取器。完成动作直接冻结值，调用同一个 `SnapshotCore` 和原状态键编码；候选不持有 lane、读取器、模型池或祖先链。
- `SimulationSnapshot` 拥有冻结候选；旧政策确实读取模型时才创建并缓存派生兼容图。该图不回写权威值，`ReleaseSimulator` 同时释放两个引用。`HasSimulator` 此时表示快照仍拥有可用状态，不能拿它统计物理旧图数量。
- 并行 `ReplayForkSeed` 可以移交不可变候选和私有死亡集合；成功紧凑回放不创建旧 Fork。旧选牌挂起路径仍可消费该 seed 并创建私有兼容图。
- 缺少计划选择是明确挂起边界，仍由旧引擎提供精确请求、评分及后续选择协议；不是吞掉执行错误或跳过候选。回退前不消费逻辑计数或 seed。完整回合的紧凑分支不实际恢复旧回合前缀，因此物理前缀计量不伪装成逻辑预算。
- 完成快照没有选牌请求时，两处 primary-choice 检查不再读取旧图。候选纯效果判定读取事件派生的最后非纯历史索引，保持旧四种纯历史类型的定义。

## 验证与结果

Release 构建 11.11 秒，零警告／错误。第一次请求因启动脚本默认注入铁甲打击且未清空跑局牌组，准入按预期显式拒绝；修正为与原输入相同的 `--cards-json '[]' --clear-run-deck` 后，复用同一产物运行。

`306eb9766ce946ee84d5423dbf5b6124` 通过，8 个固定 worker，完整搜索两种后端的 44 步路线、最终全部快照字段、分数和 fixture 列出的逻辑搜索计数逐项一致：展开 16,413、转移 134,054、选牌分支 77,969，第 8 回合胜利，搜索预计战损 11。实际战斗捕获前后严格不变。该路线此前已有[完整原生验证](simulation-full-route-20260911.md)，本请求只运行后台 A/B，不再次部署。

| 本次 headless 样本 | 旧后端 | 紧凑候选接入 |
|---|---:|---:|
| 搜索墙钟 | 12.772 秒 | 39.934 秒 |
| 完成紧凑回放 | — | 111,401 |
| 缺选择、回退旧挂起表示 | — | 22,654 |
| 兼容图物化 | — | 74,683 |

当前接入明显变慢，不能作为性能优化完成证据。完整事件投影和未迁移消费者仍有较大成本；后续应根据实际调用证据继续减少兼容图及历史重扫，再做搜索质量与成本对照。没有改变 Beam、选择预算或搜索公式来隐藏成本。配置断言保留 NoGC 16 GB，但该 fixture 直接调用 coordinator，没有进入 Runtime NoGC 生命周期。

结构门禁同步维护 Bash／PowerShell：只允许一个 Search 回放接缝使用值执行器，保持 Runtime 禁止选择、值内核无 Model 依赖、读取绑定只做单向导入、根 Fork 窄锁和候选释放边界。Linux 门禁执行，PowerShell 仅同步维护。

[结构化输入与结果](simulation-search-backend-20260911.json)保留完整原输入与两次搜索摘要。所有性能数字仅为此 headless 初始接入样本。
